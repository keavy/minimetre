#intro.clearfix
  #text.left
    %p Minimetre is closed to new projects.

    %p This is a selection of past projects.

    %p
      Feel free to
      = mail_to "hello@minimetre.com", "get in touch", :encode => :hex

  - if @random
    #quote.right
      %blockquote= @random.message
      %cite= @random.from